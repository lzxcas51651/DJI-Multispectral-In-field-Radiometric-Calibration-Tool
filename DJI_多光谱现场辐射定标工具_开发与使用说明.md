# DJI 多光谱现场辐射定标工具：开发与使用说明

## v2.0：定标布编号和反射率记忆

1. 在 RGB 图片绘制矩形或多边形 ROI。
2. 弹窗选择 Panel-01～Panel-10，也可输入自定义定标布编号。
3. 默认统一设置：在“统一反射率”输入小数（50% 输入 0.5），自动用于所有波段。
4. 若证书给出不同的波段反射率，取消“所有波段使用同一反射率”，分别填写。
5. 点击确定，添加 ROI 并记忆此编号的反射率、统一/分别模式以及最近选择。取消不会写入预设。切换编号会加载该编号上次确认的值，尚未确认的输入不保留。
6. 下次绘制 ROI 或重启程序，选择相同编号即可复用；已添加的 ROI 保留原来的值，不随预设更改。

P4M、M3M 的预设按波段组合独立保存，均可设置至少十块定标布。数据保存在当前 Windows 用户设置（注册表 `HKEY_CURRENT_USER\Software\DJI-Local-Tools\Radiometric-Calibrator`）；清空影像任务不会删除预设。不同电脑/Windows 用户不自动共享。系数 JSON 仍保存本次 ROI 实际使用的反射率，可用于复核。

注意：统一设置只适合各波段确实可采用相同已知反射率的定标布；有逐波段证书时优先使用证书值。新批次应检查预设是否仍对应同一块布。

本工具用于 DJI Phantom 4 Multispectral（P4M）和 DJI Mavic 3 Multispectral（M3M）的现场反射率板 ROI 管理、定标系数计算，以及将 WebODM 输出的 DN 多波段正射 GeoTIFF 转换成 Float32 反射率 GeoTIFF。

它是独立 Windows 桌面程序，不要求 WSL、Docker 或 WebODM 正在运行。WebODM 只负责先生成 DN 正射影像。

当前版本：1.0.1。独立仓库：[DJI-Multispectral-In-field-Radiometric-Calibration-Tool](https://github.com/lzxcas51651/DJI-Multispectral-In-field-Radiometric-Calibration-Tool)。文中的工程路径只是示例，源码运行时请替换为自己的仓库目录。

## 1. 当前实现

```text
打开 P4M/M3M 批次
  → 自动识别传感器和曝光组
  → 左侧只生成 RGB 缩略图
  → 人工浏览，或按需点击“自动查找定标布”
  → 只在 RGB 上绘制矩形或多边形 ROI
  → 输入定标布编号和各多光谱波段反射率
  → 添加任意数量的照片与 ROI
  → 点击计算时执行 RGB→各多光谱波段配准
  → 自动映射 ROI 并统计各波段 DN
  → 拟合逐波段定标模型
  → 保存 radiometric_calibration_coefficients.json
  → 选择 WebODM DN 正射影像并映射波段
  → 输出 reflectance_orthophoto.tif
```

当前版本具备：

- P4M/M3M 自动识别，也可在打开前手动指定；
- 直接读取照片 EXIF/XMP 中的 Make、Model、BandName 和 CaptureUUID；手动拍摄的定标布照片不需要航线信息文件；
- 按 Capture/文件名组织 RGB 与窄波段；
- 左侧只显示 RGB 缩略图，文件名显示在图片下方；
- 三栏宽度可拖动并在下次启动时恢复；
- 记住最近打开的批次、导入目录、项目目录和正射影像目录；
- 从批次外手动导入定标布影像；
- RGB 矩形、多边形 ROI；滚轮以鼠标位置为中心缩放，中键按住拖动；
- 计算时使用轻量 ORB-Homography，必要时使用 ECC-Affine 和归一化尺寸后备；
- `ROI-001`、`ROI-002` 自动编号，图上编号与列表联动；
- 每张照片可绘制多个 ROI，也可跨照片累计 ROI；
- ROI 均值、中位数、2% 截尾均值、标准差和 CV；
- 同一反射率水平使用零截距模型；
- 两个及以上反射率水平使用线性/Huber 稳健回归；
- 分块转换大型 GeoTIFF，输出 Float32、DEFLATE 压缩反射率影像；
- 支持中文路径；
- 保存、重新载入 ROI 和定标模型。
- “清空当前任务”：清空界面、ROI 和预览缓存，不删除磁盘上的影像或系数；无需退出软件即可重新开始。

## 2. 系数文件名

无论批次名是什么，项目文件夹内始终保存为：

```text
radiometric_calibration_coefficients.json
```

这是“辐射定标系数”的英文文件名，不添加 `M3M_20260904_1000_` 等批次前缀。JSON 内部仍记录传感器、原始批次路径、所有 ROI、统计值和逐波段模型，便于追溯。

从 1.0.1 起，直接保存到当前打开的原始批次文件夹：

```text
<原始影像批次文件夹>\radiometric_calibration_coefficients.json
```

不再创建 `_calibration` 子目录/同级目录，也不再弹出目录选择框。已有同名文件时先确认覆盖；取消不会计算或改动旧文件。保存通过临时文件写入后原子替换，避免中途写入失败损坏原有系数。源目录只读或无写入权限时会提示错误，标注仍留在窗口内；请先处理该目录的写入权限。

## 3. 自动查找不会默认运行

打开批次只执行文件名/波段扫描和后台缩略图生成，不执行定标布视觉检测。

只有点击：

```text
自动查找定标布
```

才会运行候选算法。为保证速度，当前算法：

1. 默认最多检查 160 张预览图；
2. 优先检查飞行开头 80 张和结尾 80 张；
3. 使用缩小到最多 480 像素的图像；
4. 最多使用 8 个 CPU 线程；
5. 检测规则四边形、内部均匀度、面积和亮度；
6. 最多返回 16 个候选供人工确认。

自动结果只是候选排序，不会自动创建 ROI 或直接参与定标。

## 4. Windows 源码运行

打开 PowerShell：

```powershell
cd F:\DJ_image_preprocessing

py -3.12 -m pip install -r local_tools\radiometric_calibrator\requirements-windows.txt

py -3.12 local_tools\dji_radiometric_calibrator.py
```

## 5. 基本操作

1. 点击“打开批次”，选择 P4M 或 M3M 原始影像文件夹。
2. 在左侧 RGB 缩略图中寻找定标布；需要辅助时才点击“自动查找定标布”。
3. 选择 RGB 照片，在中间工作区上方点击“矩形 ROI”或“多边形 ROI”。多边形通过单击添加顶点，双击结束。
4. 只在 RGB 图像中勾选定标布内部，不要包含边框、阴影或背景。
5. 输入定标布编号，以及定标证书中各多光谱波段的反射率（0～1）。
6. 可继续选择其他 RGB 照片或其他定标板，添加多个 ROI。
7. 点击“计算并保存系数”。此时才执行配准、ROI 映射和 DN 统计，并将系数直接保存到原始影像文件夹，不需要选择目录。
8. 点击“应用到 DN 正射影像”，选择 WebODM 的 `orthophoto.tif`。
9. 按界面逐一确认 GeoTIFF 波段映射。
10. 选择是否裁剪到 0～1，生成 `reflectance_orthophoto.tif`。

### 5.1 清空并开始下一批

点击顶部“清空当前任务”。存在标注时会提示确认；确认后左侧列表、画布、ROI、拟合结果和预览缓存清空。磁盘上的影像、已保存 JSON、正射成果保持不变，历史打开路径也保留。

随后点击“打开批次”即可选择新任务；重新选择同一文件夹就是重新处理当前批次。不必退出软件。清空后旧任务的后台缩略图或候选查找结果不会回填界面。计算期间暂不可清空，避免计算与切换任务相互干扰。

### 5.2 鼠标和性能

- 左键：按所选工具画矩形或多边形；中间工作区仅保留这两个标注工具。
- 中键按住：在任何标注模式下拖动画布，无需“平移”按钮。
- 滚轮：围绕鼠标位置缩放，设有上下限。
- 拖动或缩放时暂用快速插值，停止约 140 毫秒后恢复平滑显示；这是为了交互流畅，不影响原始 DN 计算。
- JPEG 缩略图直接缩放解码；工作区缓存最近三张预览，刷新 ROI 时不再重复解码原图。
- 预览最长边不超过 2400 像素，ROI 坐标会按横纵比例换算回原图；统计与定标依旧使用原始多光谱数据，而不是缩略图。

本机在同一张 M3M RGB 上的简短测试中，旧的双次完整解码约 266 ms，新预览解码约 161 ms；同图重复选择复用缓存。该值不代表所有电脑的帧率，机械硬盘、网络目录、显卡驱动和图像大小仍会影响体验。

左侧列表和中间工作区始终只显示 RGB，不再要求用户逐波段重复勾选。配准结果、质量分数、映射后的波段多边形和 DN 统计会写入系数 JSON。ORB 特征足够时使用透视变换；跨光谱特征较弱时使用 ECC 仿射配准；两者都失败时才使用归一化图像坐标后备，并在完成对话框中明确提示需要重点复核。

M3M 的 RGB 与多光谱相机不是同一个光心。定标布距离无人机很近、画面透视很强或遮挡明显时，全局自动配准可能受视差影响。因此应尽量让定标布位于画面中部、保持完整清晰，并检查 JSON 中的 `registration_method` 与 `registration_score`。

手动拍摄的定标布照片可以直接导入，不要求存在航线、任务或航点文件。程序优先从照片自身的 EXIF/XMP 判断 P4M/M3M 和波段；如果导入照片的传感器与当前项目不一致，会显示阻止性警告。元数据被图片软件清除时，程序才要求用户手动指定波段和传感器。

识别时优先使用照片自身的以下字段：

| 元数据 | 用途 |
| --- | --- |
| `Make` | 判断制造商是否为 DJI |
| `Model` | `FC6360` 识别为 P4M，`M3M` 识别为 M3M |
| `BandName` | 判断 Blue、Green、Red、RedEdge、NIR 或 RGB |
| `CaptureUUID` | 将同一次曝光的不同波段归为一组 |

若 `CaptureUUID` 缺失，程序才使用 DJI 标准文件名中的时间、序号等信息进行后备分组。不要先用会清除 XMP 的图片软件“另存为”；确需另存时，应同时保留未经修改的原片。

## 6. 定标模型

只有一个已知反射率水平时：

```text
ρ = a × DN
```

有两个以上不同反射率水平时：

```text
ρ = a × DN + b
```

同一块定标布拍摄很多次可以降低随机误差，但不能代替多个不同反射率水平来估计截距。

当前模型输入是 WebODM `radiometric-calibration=none` 对应的原始 DN 域。正式使用应保证飞行和定标布照片使用相同、固定的曝光与增益，并在 WebODM 配置中关闭全局接缝亮度均衡、使用 DEFLATE 无损压缩。自动曝光或明显云影变化时，不应把一个原始 DN 系数直接用于整个批次。

## 7. 构建 Windows EXE

需要可安装、修复、卸载的单个 MSI 安装包时，见 [安装包构建与维护](DJI_辐射定标工具_安装包构建与维护.md)。下面仍保留文件夹免安装版的构建方法。

在 Windows PowerShell 中：

```powershell
cd F:\DJ_image_preprocessing

powershell -ExecutionPolicy Bypass -File `
  local_tools\radiometric_calibrator\build_windows_exe.ps1
```

输出目录：

```text
local_tools\radiometric_calibrator\dist\DJI_Radiometric_Calibration_Tool\
```

把整个目录复制到其他 Windows 10/11 64 位电脑即可运行。不要只复制单个 EXE，因为 Qt、GDAL/Rasterio 等运行库位于同一目录中。

构建脚本会使用独立的 `.build-venv`，并使用不携带另一套 Qt DLL 的 `opencv-python-headless`。它还会排除构建主机 PATH 中可能被误收集的 Poppler ICU DLL，防止出现 `DLL load failed while importing QtCore`。更新版本时应删除旧发布目录后复制整个新目录，不能只用新 EXE 覆盖旧目录中的部分 DLL。

## 8. 测试

不启动 GUI 的核心测试：

```powershell
cd F:\DJ_image_preprocessing\local_tools

py -3.12 -m unittest tests.test_radiometric_calibrator -v
```

本工程还使用 `MS_test_data` 中的两批实测数据做过只读验证：

- `102MEDIA`：从照片 XMP 的 `Model=FC6360` 识别为 P4M，识别 2 组完整五波段曝光；
- `20251226_6050_M_1`：从照片 XMP 的 `Model=M3M` 识别为 M3M，并能利用元数据/拍摄序号组织手动拍摄的 RGB 与多光谱照片；识别到 4 组完整四波段曝光及不完整的单波段照片。

这两项验证均不读取航线文件。实测数据只用于测试，不会被打包到 EXE。

## 9. 当前边界与后续升级

- 当前自动候选算法是快速传统视觉算法，不保证识别任意外观的定标布；
- 尚未加入 AprilTag/ArUco 定标板模板；
- 已实现 RGB ROI 到各窄波段的自动几何投影，但近距离双相机视差仍需人工复核质量；
- 尚未实现逐幅曝光、增益、黑电平和暗角归一化；
- 尚未生成独立 HTML/PDF 质量报告；
- 已支持标准 EXIF/XMP 元数据优先识别；少数非标准私有标签可在后续版本通过捆绑 ExifTool 继续扩展；
- 反射率光谱曲线 CSV 与波段响应积分尚未加入。

因此当前版本适合作为可运行的人工 ROI 与 DN 经验线定标第一版。严格跨日期定量分析前，需要使用已知测试数据验证系数、波段顺序和输出反射率。
