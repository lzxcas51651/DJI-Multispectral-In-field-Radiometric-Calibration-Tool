# 更新日志

## 2.2.0

- 自动查找得到候选后，左侧立即筛选为候选 RGB 图片，并可在“全部 RGB 影像/定标布候选”之间切换。
- DN 正射影像的默认输出改为输入文件同目录，名称为原文件名加 `_ref`。

## 2.1.1

- 接受固定光谱波段之后附带一个 Alpha；通过 TIFF ColorInterp 或波段描述识别，仅定标和输出光谱波段。
- 计算完成及载入项目后，系数显示统一为 P4M/M3M 标准波段顺序，不修改系数或像素顺序。

## 2.1.0

- P4M / M3M 波段映射窗口按固定输入顺序显示，不交换或重排波段；校验波段数和系数完整性。
- 输出保留空间参考、文件及波段标签、自定义元数据；原始 DN 属性作为 SOURCE_METADATA 来源记录保存。
- 保留后台进度条和原子输出保护，重新打包 Windows EXE/MSI。

## 2.0.1

- 输出反射率波段按输入波段编号排序，记录原波段编号，避免系数顺序改变成果波段顺序。

- 系数计算和 DN 正射反射率转换移到后台线程，显示步骤和百分比进度。
- 波段对应关系集中在同一窗口；实时检查漏选、重复，未知顺序不猜测。
- DN 转换使用固定大小处理块和原子替换，保护输入影像及已有输出。
- 增加线程响应、波段映射校验、进度和失败输出保护回归测试。

## 2.0.0

- 定标布编号扩展为 Panel-01～Panel-10，支持自定义编号。
- 默认所有波段统一反射率，可切换为逐波段输入。
- 确认 ROI 后持久化编号、反射率、填写模式和最近选择；取消不保存。
- 不同传感器波段组合分别记忆，现有 ROI 不被预设更新改写。
- 更新使用说明、自动回归测试和 MSI 版本，保留原升级标识。

## 1.0.1

- 新增“清空当前任务”，释放列表、ROI、模型和预览缓存；保留原始影像、系数文件及历史路径。
- 旧任务后台结果在任务清空/切换后不再回填，关闭时等待工作线程安全结束。
- 移除平移按钮，统一使用鼠标中键拖动；默认矩形 ROI。
- JPEG 缩放解码、三张预览缓存、交互快速插值/静止恢复平滑显示，减少重复解码和缩放抖动。
- 固定在原始批次目录保存 `radiometric_calibration_coefficients.json`，取消预创建目录和目录选择框。
- 已有系数先询问覆盖；采用临时文件+原子替换。
- 更新 MSI 版本、独立 README、使用说明及 GUI 回归测试；分发包含依赖许可证元数据。

## 1.0.0

- P4M/M3M 识别、RGB 标注、按需候选查找、轻量跨波段配准和定标系数计算。
- Float32 反射率 GeoTIFF 转换。
- WiX MSI 安装、修复、卸载、升级工程。
