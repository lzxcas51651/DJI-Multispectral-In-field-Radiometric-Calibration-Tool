# 更新日志

## 1.0.1

- 新增“清空当前任务”，释放列表、ROI、模型和预览缓存；保留原始影像、系数文件及历史路径。
- 旧任务后台结果在任务清空/切换后不再回填，关闭时等待工作线程安全结束。
- 移除平移按钮，统一使用鼠标中键拖动；默认矩形 ROI。
- JPEG 缩放解码、三张预览缓存、交互快速插值/静止恢复平滑显示，减少重复解码和缩放抖动。
- 固定在原始批次目录保存 `radiometric_calibration_coefficients.json`，取消预创建目录和目录选择框。
- 已有系数先询问覆盖；采用临时文件+原子替换。
- 更新 MSI 版本、独立 README、使用说明及 GUI 回归测试；分发包含依赖许可证元数据。

## 1.0.0

- P4M/M3M 识别、RGB 标注、按需候选查找、轻量跨波段配准和定标系数计算。
- Float32 反射率 GeoTIFF 转换。
- WiX MSI 安装、修复、卸载、升级工程。
